import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'M08-UF1-PR01-AliceAliagaMoreno',
  webDir: 'www'
};

export default config;
